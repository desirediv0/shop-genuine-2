# Points the app at your backend. Leave unset in development and the app will
# auto-detect the machine running Metro (see src/api/config.ts).
#
# Android emulator:   http://10.0.2.2:4000
# iOS simulator:      http://localhost:4000
# Physical device:    http://<your-LAN-ip>:4000
# Production:         https://api.shopgenuine.online
EXPO_PUBLIC_API_URL=
